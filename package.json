{
  "name": "blog-env",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "colors": "^1.1.2",
    "cross-env": "^5.0.5",
    "dotenv": "^4.0.0",
    "prompt": "^1.0.0",
    "scp2": "^0.5.0"
  },
  "scripts": {
    "install": "cd jekyll && bundle install",
    "deploy": "yarn build && node run-deploy.js",
    "build": "cd jekyll && bundle exec jekyll clean && bundle exec jekyll build",
    "dev": "cd jekyll && bundle exec jekyll clean && bundle exec jekyll serve --watch --drafts",
    "clean": "cd jekyll && bundle exec jekyll clean"
  }
}
